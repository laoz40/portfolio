<div
	class="section-media-gallery"
	aria-hidden="true">
	<div class="section-media-tile"></div>
	<div class="section-media-tile"></div>
	<div class="section-media-tile"></div>
	<div class="section-media-tile"></div>
</div>

<style>
	.section-media-gallery {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		width: 100%;
		gap: 0.75rem;
		align-self: center;
		transform: rotate(var(--section-media-tilt, 0deg));
	}

	.section-media-tile {
		position: relative;
		min-height: clamp(7rem, 16vh, 10rem);
		background: var(--color-accent-lime);
		border: 8px solid var(--color-surface-page);
		box-shadow: var(--shadow-paper-medium);
	}

	.section-media-tile::before {
		content: "";
		position: absolute;
		top: 0;
		left: 50%;
		width: var(--size-project-tape-width);
		height: var(--size-project-tape-height);
		transform: translate(-50%, -50%) rotate(1deg);
		background: var(--color-tape);
		pointer-events: none;
	}

	@media (max-width: 1240px) {
		.section-media-gallery {
			grid-template-columns: 1fr 1fr;
		}
	}

	@media (max-width: 640px) {
		.section-media-gallery {
			grid-template-columns: 1fr;
		}
	}
</style>
